---
import { hero, contact } from '../data/site';
---

<section class="hero">
  <div class="container-page">
    {/* The headline is the first thing on the page. There was a metadata rule
        above it, but a second band of small-caps type directly under the site
        header just read as a second header. */}
    <h1 class="hero-headline display">
      {
        hero.headline.map((line) => (
          <span class="hero-line">
            <span class="hero-line-inner" set:html={line} />
          </span>
        ))
      }
    </h1>

    <div class="hero-foot">
      <p class="hero-lede">{hero.lede}</p>

      <div class="hero-actions">
        <a href="#work" class="action">
          The index
          <span class="action-rule" aria-hidden="true"></span>
        </a>
        <a href={`mailto:${contact.email}`} class="action">
          Email
          <span class="action-rule" aria-hidden="true"></span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: clamp(4rem, 11vw, 9rem);
    padding-bottom: clamp(3rem, 7vw, 5rem);
  }

  .hero-headline {
    /* Big. This is the one place on the site where type is the entire design. */
    font-size: clamp(2.75rem, 10vw, 8.5rem);
    max-width: 13ch;
  }

  .hero-line {
    display: block;
    overflow: hidden;
  }

  .hero-line-inner {
    display: block;
    animation: line-rise 1.05s var(--ease-out-expo) both;
  }

  .hero-line:nth-child(2) .hero-line-inner {
    animation-delay: 0.09s;
  }

  .hero-line:nth-child(3) .hero-line-inner {
    animation-delay: 0.18s;
  }

  /* Emphasis inside the headline, marked up in the data file. Color alone —
     an italic at this width and weight fights the rest of the line. */
  .hero-headline :global(em) {
    font-style: normal;
    color: var(--accent);
  }

  .hero-foot {
    display: grid;
    gap: 2rem;
    margin-top: clamp(2.5rem, 6vw, 4rem);
    padding-top: 1.5rem;
    border-top: 1px solid var(--rule);
    animation: fade-in 0.9s var(--ease-out-expo) 0.4s both;
  }

  @media (min-width: 900px) {
    .hero-foot {
      grid-template-columns: minmax(0, 1fr) auto;
      align-items: start;
      gap: 4rem;
    }
  }

  .hero-lede {
    max-width: 46ch;
    font-size: clamp(1rem, 1.5vw, 1.125rem);
    line-height: 1.65;
    color: var(--ink-muted);
  }

  .hero-actions {
    display: flex;
    gap: 2rem;
  }

  /* Text links with a rule underneath, not buttons. */
  .action {
    position: relative;
    font-size: 0.9375rem;
    font-weight: 500;
    padding-bottom: 0.4rem;
    white-space: nowrap;
  }

  .action-rule {
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 1px;
    background: var(--ink);
  }

  .action-rule::after {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--accent);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s var(--ease-out-expo);
  }

  .action:hover {
    color: var(--accent);
  }

  .action:hover .action-rule::after,
  .action:focus-visible .action-rule::after {
    transform: scaleX(1);
  }

  @keyframes line-rise {
    from {
      transform: translateY(102%);
    }
  }

  @keyframes fade-in {
    from {
      opacity: 0;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-line-inner,
    .hero-foot {
      animation: none;
    }
  }
</style>
